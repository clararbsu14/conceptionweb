# ── Database ─────────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=autoloc

# ── Auth ─────────────────────────────────
JWT_SECRET=change-me

# ── Server ───────────────────────────────
PORT=3000

# ── Email (Gmail SMTP via nodemailer) ────
# Use a Gmail App Password (16-char). Requires 2-Step Verification on the Google account.
# See: https://myaccount.google.com/apppasswords
EMAIL_USER=votre.email@gmail.com
EMAIL_PASS=xxxx xxxx xxxx xxxx
EMAIL_FROM=AUTOLOC' <votre.email@gmail.com>
